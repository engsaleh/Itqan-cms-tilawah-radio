# تطبيق راديو التلاوات 📻 

تطبيق راديو التلاوات هو تجربة تطبيقية تعليمية، تهدف إلى استكشاف إمكانيات الواجهة البرمجية  [Itqan Quranic CMS API](https://api.cms.itqan.dev/developers-api/docs) وبناء نماذج استخدام عملية لها.

تم تطوير المشروع كتطبيق ويب يقدّم تجربة استماع قرآنية متتابعة (Radio / Playlist)، بواجهة حديثة مستوحاة من تطبيقات البث الصوتي، مع الحفاظ على بساطة الاستخدام والتصميم الموافق لقدسية المحتوى القرآني.

## فكرة التطبيق

يوفّر التطبيق محطة استماع قرآنية تعمل تلقائيًا، حيث يقوم بـ:

اختيار الرواية (حاليًا تتوفر تلاوات لرواية حفص)

اختيار القارئ (يتوفر حاليًا عدد من القرّاء)

جلب جميع سور المصحف الخاصة بهذه التلاوة

تشغيلها تلقائيًا من سورة الفاتحة حتى سورة الناس دون انقطاع

التجربة مصمّمة لتكون سلسة وواضحة، دون الحاجة لأي تدخل من المستخدم بعد بدء التشغيل.

الميزات

🎧 تشغيل تلقائي ومتسلسل لجميع السور

📖 عرض اسم السورة ورقمها مباشرة من الواجهة البرمجية

🌙 دعم الوضع الداكن والفاتح

📦 جلب كامل التلاوات مع معالجة الترقيم الصفحي (Pagination)

🧩 كود بسيط وواضح باستخدام Vanilla JavaScript

🌐 دعم تعدد اللغات عبر Accept-Language

الواجهة البرمجية المستخدمة
الرابط الأساسي BASE API URL (محدَّث)
https://api.cms.itqan.dev


⚠️ ملاحظة:
الرابط القديم
https://api.cms.itqan.dev/developers-api
ما زال مدعومًا حاليًا، لكن سيتم إيقافه لاحقًا.

أهم المسارات المستخدمة

الروايات
```
GET /riwayahs/
```

القرّاء
```
GET /reciters/
```

التلاوات (الأصول)
```
GET /recitations/
```

المسارات الصوتية للتلاوة
```
GET /recitations/{recitation_id}/
```
اللغة والترجمة

تعتمد الواجهة البرمجية الآن على ترويسة HTTP التالية:
```
Accept-Language: ar | en
```

يتم إرجاع الحقول النصية الموحدة (name, surah_name) حسب اللغة المطلوبة

اللغة العربية هي الافتراضية

جلب كامل التلاوات (Pagination)

يعيد مسار التلاوات عددًا محدودًا من العناصر في كل صفحة، لذلك تم تطبيق حل برمجي لجلب جميع الصفحات تلقائيًا اعتمادًا على الحقل count.

مثال عملي
```
const API = "https://api.cms.itqan.dev";

async function fetchAllTracks(recitationId) {
  let page = 1;
  let all = [];
  let total = Infinity;

  while (all.length < total) {
    const res = await fetch(
      `${API}/recitations/${recitationId}/?page=${page}`,
      {
        headers: {
          "Accept-Language": "ar",
        },
      }
    );

    const data = await res.json();
    all.push(...(data.results || []));
    total = data.count ?? all.length;
    page++;
  }

  return all;
}
```
بنية البيانات المعتمدة (بعد التحديث)

توحيد الحقول:
```
{
  "id": 1,
  "name": "حفص عن عاصم"
}
```

أسماء السور:
```
{
  "surah_number": 1,
  "surah_name": "الفاتحة"
}
```

معلومات إضافية على مستوى السورة:
```
revelation_place

revelation_order

ayahs_count
```
تقصير وتوحيد رابط الصوت:
```
{
  "audio_url": "https://..."
}
```
التقنيات المستخدمة

HTML

CSS (دعم الوضع الداكن والفاتح)

JavaScript (Vanilla)

تشغيل المشروع

حمّل المستودع أو انسخه

افتح ملف index.html مباشرة في المتصفح

سيبدأ التطبيق بجلب البيانات من الواجهة البرمجية

اختر الرواية والقارئ ثم ابدأ تشغيل الراديو

🔗 نسخة تجريبية مباشرة:
https://engsaleh.github.io/Itqan-cms-tilawah-radio/


## لقطات الشاشة Screenshots

> واجهة التطبيق في الوضع الداكن والفاتح

### الوضع الداكن

![Dark Mode](./screenshots/dark-mode.png)

### الوضع الفاتح

![Light Mode](./screenshots/light-mode.png)

## المساهمة

المشروع مفتوح للمساهمات والأفكار.

في حال وجود ملاحظات أو اقتراحات:

* افتح Issue
* أو أرسل Pull Request

## شكر وتقدير 🙏

الشكر لفريق Itqan CMS على إتاحة تلاوات قرآنية رسمية عالية الجودة عبر واجهة برمجية منظمة تخدم المطورين وتفتح آفاقًا جديدة للتطبيقات القرآنية.
